# Design: Founders' Desk — Founder Equity Calculator

Generated by /office-hours on 2026-09-02
Branch: feat/payload-cms-integration
Repo: HeavenlyEntity/my-next-spotlight
Status: APPROVED
Mode: Startup
Implementation plan (locked, supersedes engine details here): docs/plans/founders-desk-equity-calculator.md

## Problem Statement

Technical people who build a company's product routinely accept equity sized for an early employee while doing founder-level work, because nothing in front of them says the number is wrong. Add a "Founders' Desk" menu to the AMWARE site whose first tool is a founder equity calculator built for one seat: it classifies the person's work as founder / founding executive / hired executive, states a defensible fully diluted ownership range for that work at that stage, shows the gap to the offer, models the stake through each funding round on the company's declared exit path (bootstrap, acquisition, IPO), prices banked part-time work and a salary cut on the same axis as equity, and ends in a printable negotiation brief plus a CTA to have AMWARE review the offer. Scope is CTOs, software engineers, and CEOs who build their own tech.

## Demand Evidence

- "I lived it myself." The founder (Alec) negotiated his own stake and was hurt by it.
- The scar, in his words: "Undervalued my own contribution." He built most of the tech and ran operations but accepted a stake sized like an early employee, not a founder.
- The consequence surfaces years later at a funding round, which is why the pain doesn't generate search traffic today: people don't know they're underpaid until dilution makes it obvious.
- This is a sample of one. The assignment below is to collect three more real offers before the engine is written.

## Status Quo

- "They accept what's offered." No tool. A business co-founder or a lawyer proposes a number and the engineer says yes.
- Existing calculators (Capbase, Gust, BVJ Consulting, Alumni Founders, MoonshotNX, Valuefy, foundrs) are multi-founder _split_ tools: they divide 100% among a team by scoring idea, capital, commitment, expertise, network, and track record. None classify founder vs hire, none price salary against equity, none handle banked part-time work, and none distinguish an IPO path from an acquisition or bootstrapped exit. Only Alumni Founders projects dilution to exit dollars (Carta 2026 medians).
- Benchmarks (Ravio, Index Ventures, founder-math, Stock Option Counsel) split the world into "founding CTO" (20–40%) and "hired CTO" (seed 1–5%, Series A 1–3%) with nothing in between. The target user lives in that gap.
- Conclusion: the tool replaces silence, not a worse tool. Its job is to make an under-sized offer visibly wrong in under a minute.

## Target User & Narrowest Wedge

**The human:** a fractional CTO converting to full-time CTO at a pre-seed company. They have months of part-time work banked (often deferred or unbilled), a salary cut on conversion, and ambiguous founder status at the stage where dilution is steepest. An offer is on the table and they must answer this week.

**The wedge:** the conversion moment. Everything else (co-founding at formation, hired after formation, founding engineer, CEO-who-builds) is supported by the same engine and the same wizard, but copy, defaults, and marketing lead with the conversion.

**The realization the tool exists to create:** "You're being sized as a hire while doing founder work." That sentence, with the range to say out loud under it, is the product. No letter grade is rendered anywhere; the headline sentence is the opening.

## Constraints

- Next.js 15 App Router, React 19, JavaScript (no TS in app code), Tailwind 4, shadcn (`components.json` tsx:false, radix-ui umbrella), `motion/react` only in client components. pnpm.
- amw design system (`src/styles/storefront.css`): tokens exist only inside `.amw`; portaled content must carry the class.
- Template grammar from the minimal template: `SectionEyebrow` + Layer headline, `max-w-6xl`, muted `rounded-2xl` panels, fadeInUp on view, chevron CTA.
- ~~No account, nothing stored, nothing sent (premise P4).~~ **SUPERSEDED 2026-09-04 by decision D3 of the job offer calculator plan**: the shared answers now persist to `sessionStorage` so they survive a reload and cross between the two tools, with a clear button on both. Nothing is _sent_ — that half still holds, and the copy on every surface now says "nothing leaves your browser". No URL-encoded state (an offer pasted into Slack is a privacy foot-gun). No event tracking of calculator inputs in v1; the site-wide Vercel `<Analytics />` and `<SpeedInsights />` in `(site)/layout.tsx` still record a pageview for `/founders/equity`. The results copy promises "Nothing you type leaves your browser" and that must be literally true: no request payload may contain an input.
- ~~Never run `pnpm build` while the dev server is on; verify with `tsc --noEmit`, per-file `next lint`, prettier.~~ **SUPERSEDED 2026-09-04 by the Next 16 upgrade**: dev and build have separate output directories now, so concurrent builds are safe, and `next lint` was removed. Verify with `npx eslint <path>`, `tsc --noEmit`, prettier.
- Numbers must cite a source and a "last updated" date. Every result page carries "not legal or financial advice."
- Nav lives in `src/components/AppHeader.jsx` (`PillNav` items). `PillNav` renders every submenu item as a `<Link>` keyed by `href`, so nothing goes in the dropdown without a real route.
- The contact form (`src/app/(site)/contact/ContactForm.jsx`) posts name / email / subject / message to `/api/contact-submissions`; the `ContactSubmissions` collection has exactly those fields plus status.

## Premises

1. **Single seat, founder-vs-hire first.** The user is one person evaluating one offer, not a team splitting a pie. The first output is the classification (founder / founding executive / hired executive / hire) and the band it implies. _Agreed._
2. **Percent range first, exit dollars as scenarios.** _Revised after Codex's challenge._ The headline is a present-day fully diluted ownership range and the gap to the offer, because that is the number the person can defend in the room. Exit value is shown one screen later as conservative / base / upside scenarios under the declared path; the IPO path changes how many dilution rounds sit between today and the exit, which is where "20% at an IPO-bound company is a bigger ask" is made visible.
3. **Salary and banked work price on the same axis.** The salary cut on conversion and the fractional work already delivered (foregone fees at market rate) both convert into equity points at a stage-specific exchange rate, shown as separate adjustments so the person can negotiate each one.
4. **Lead magnet: no account, nothing stored.** Runs in the browser, ends with a printable brief and a CTA to have AMWARE review the actual offer. _Amended 2026-09-04 (D3): stored in the tab, never sent. See the Constraints note above._

## Cross-Model Perspective

Codex (read-only, structured summary, no conversation access):

- Steelman: "A single-seat compensation negotiator for technical operators converting into founder roles. It classifies founder-vs-hire status, prices banked work and salary sacrifice, then shows a defensible ownership range, dilution-adjusted outcomes, and concrete negotiation language."
- Most revealing answer: "Fractional CTO converting to full-time Pre-seed CTO." Build specifically for this conversion, not a generic calculator for every engineer and CEO.
- Challenged premise: P2 as originally stated ("dollars at exit, not percent"). "Exit dollars compound speculative valuation, timing, dilution, preferences, and tax assumptions into false precision. Lead with a present-day fully diluted equity range and compensation gap; show exit values as scenarios." Accepted; P2 revised above.
- 48-hour prototype: client-side wizard; inputs for role/stage, product and ops work, decision authority and risk, fractional duration/hours/rate, offered vs market salary, offer and vesting, exit intent; outputs classification, fully diluted range, banked-work and salary adjustments shown separately, ownership after Seed/A/B/C, conservative/base/upside exit values, "below/within/above range," a negotiation brief with an AMWARE CTA; shareable compressed-URL state; unit tests for rules and Playwright for the main flow. Skip CMS, accounts, cap-table modeling, taxes, preferences, legal docs, payments, non-technical roles.

Synthesis: agreement on the wedge, the classification, and the prototype shape. Disagreement on shareable URLs (rejected for privacy; print/save instead). Codex's skip list is adopted; CMS-managed benchmarks stays an open question for after the first refresh.

## Approaches Considered

### Approach A: One-page calculator

Single client route, form plus live results, hard-coded benchmarks, one nav item. Rejected: fifteen inputs on one screen overwhelms the first-time founder, and there is no home for the second tool.

### Approach C: Offer Grader

Enter the offer plus five yes/no questions, get a letter grade and the one number to say; full calculator behind "show the math." Rejected: a grade can feel glib for a contract decision. What survives is the idea of leading with one sentence; no grade is rendered.

### Approach B: Founders' Desk product surface (chosen)

A nested route under `(site)` with a desk landing, a guided wizard with a persistent results rail, a pure tested calculation engine, versioned sourced benchmark data, and a printable negotiation brief ending in a pre-filled contact CTA.

## Recommended Approach

### Information architecture (approved wireframe v2)

1. **Nav:** new "Founders' Desk" dropdown in `AppHeader.jsx` beside "Catalog", same shape (`label`, `submenu.title`, `items[]` each with `label`, `href`, `kicker`, `desc`). `PillNav` has no dropdown-level kicker or desc slot. Only routes that exist go in it:
   - `submenu.title`: "// FOUNDERS' DESK — TOOLS FOR TECHNICAL FOUNDERS" (Catalog's format).
   - Item: "Equity calculator" → `/founders/equity`. Kicker: "/founders/equity · no account". Desc: "Are you being sized as a hire while doing founder work?"
   - Item: "Review my offer" → `/contact?topic=offer-review`. Kicker: "/contact · offer review". Desc: "Send the offer, get a read from AMWARE."
   - Tools that don't exist yet appear only on the desk landing, not in the nav. No change to `PillNav` is required.
2. **Desk landing** `/founders`: left sidebar listing every tool with a lucide mark (live highlighted; Dilution modeler, Salary ⇄ equity, Offer glossary, Vesting timeline rendered as non-link `aria-disabled` rows with a "soon" kicker) and a "Get help → Review my offer" entry; main column with `<SectionEyebrow index="00" label="Founders' Desk" />` (renders `// SEC.00 / Founders' Desk`; the `/` separator is the component's), headline "Tools for the person who builds the thing.", a tool grid (live first, soon dimmed). Below `lg` the sidebar becomes a horizontal chip row above the grid.
3. **Wizard** `/founders/equity`, four steps, one question group per step, sticky results rail on the right at `lg` and up:
   - **Step 1 · Who you are:** role switch (CTO / Software engineer / CEO who builds) as three cards; how you're joining (co-founding at formation / converting from fractional or contract / hired after formation); company stage (idea, pre-seed, seed, Series A, Series B+); declared exit path (bootstrap or hold / acquisition / IPO / don't know). When joining = formation, a "How many founders, including you?" stepper appears (default 2, min 1, max 6). All step-1 fields are required to continue; defaults are pre-selected for the wedge (CTO, converting, pre-seed, IPO).
   - **Step 2 · The work:** responsibility chips tailored to the role (sets and weights below). Only when joining = converting from fractional or contract: sliders for months in the role (0–36, default 6), average hours per week (0–60, default 10), market rate per hour (50–500, default 150), each with a paired `<input type="number">` synced two-way (Radix Slider has no text input), plus an exact typed field for fees actually billed (default 0), and a live line showing banked work in foregone fees. For formation and hired-after joiners those fields are not rendered: the work was salaried or is founder contribution, which the classification and band already price.
   - **Step 3 · Salary and the offer:** market salary (slider 60k–400k, default from the role × stage benchmark, paired number input), offered salary (optional; blank means no salary adjustment), offered equity percent (required to draw the gap bar; blank keeps the bar empty with copy), instrument (options / restricted stock / unsure), vesting years (default 4) and cliff months (default 12); both feed the brief only: cliff > 12 months or vesting > 4 years is flagged as a negotiation point. No valuation or option-pool inputs in v1.
   - **Step 4 · Your read (results / negotiation brief):** classification badge plus one-line gloss; headline sentence; fully diluted range vs offer with a gap bar and "the number to say"; adjustments shown separately (banked work, salary); stake through the rounds as a bar timeline; exit scenarios under a path toggle; "Ask sanity" line comparing the same offered percent under acquisition vs IPO; buttons: Have AMWARE review your offer, Print / save brief, Show the math; sources footer with "benchmarks last updated" and the disclaimer. The results screen uses `animate` (not `whileInView`) so nothing is stuck at opacity 0 when printed.
4. **Results rail** (steps 1–3): "Your read so far": classification (the "Founder" badge from step 1 when joining = formation; "?" until step 2 for the other two joining paths), band, a gap bar that stays empty until the offer is entered, and a checklist of what's known vs still pending. At step 1 the band is the stage's full span labeled "possible range": from `hire.lo` to `founding_executive.hi` for the role, or the founder band when joining = formation. From step 2 it is the class band. The checklist shows "Banked work: n/a (salaried)" for joining = formation or hired_after. Below `lg` the rail collapses to a one-line summary bar pinned to the bottom of the viewport (classification + band) that expands in place into a full-height panel on tap (plain toggled panel, no sheet component). Announcements: a visually hidden `aria-live="polite"` node (not the whole rail) announces only when the classification or the below/within/above position changes, debounced 500 ms.

### Calculation engine (pure module, unit-tested)

Location: `src/lib/founders/equity/` with `benchmarks.js` (data + sources + `updatedAt`), `classify.js`, `bands.js`, `adjustments.js`, `dilution.js`, `scenarios.js`, `engine.js` (composes), and `__tests__/`. The engine uses relative imports only (no `@/` alias) so vitest needs no alias config, and it never touches the DOM.

**Inputs**

```
role: 'cto' | 'engineer' | 'ceo_builder'
joining: 'formation' | 'fractional_conversion' | 'hired_after'
founders: number            // only when joining = formation; default 2, clamp [1, 6]
stage: 'idea' | 'preseed' | 'seed' | 'series_a' | 'series_b_plus'   // idea maps to preseed for every table
path: 'bootstrap' | 'acquisition' | 'ipo' | 'unknown'
work: { months, hoursPerWeek, ratePerHour, feesBilled, responsibilities: string[] }
comp: { marketSalary, offeredSalary?, offeredEquityPct?, instrument, vestingYears, cliffMonths }
```

**Classification** (`classify.js`)

- Each responsibility chip carries a weight; every role's set sums to 8.5 so the thresholds below apply unchanged. `founderScore` = sum of selected weights.
  - CTO: architecture 1, shipped the MVP 1.5, hired engineers 1, owned the roadmap 1, ran infra and on-call 0.5, investor/tech diligence 1, ops: vendors and contracts 1, personal capital in 1.5.
  - Software engineer: designed the architecture 1.5, shipped the MVP 1.5, owned a product area end to end 1, ran infra and on-call 1, hired or mentored engineers 1, customer-facing support 0.5, ops: vendors and contracts 0.5, personal capital in 1.5.
  - CEO who builds: wrote the product 1.5, shipped the MVP 1, fundraising and investor relations 1.5, sales and first customers 1, hired the team 1, ops: finance, legal, vendors 1, personal capital in 1.5.
- `joining = formation` → always `founder`. The score positions them within the founder band: score < 3 → `[lo, lo + (hi − lo) / 3]` with gloss "co-founder with a light contribution load so far"; ≥ 3 → the full band.
- Otherwise: `founderScore ≥ 3.5` → `founding_executive` at any stage; `2 ≤ founderScore < 3.5` → `hired_executive`; `< 2` → `hire` (gloss "this reads as an employee role").
- Output includes the responsibilities that pushed the classification, for the gloss ("{selected} of {total} founder-level responsibilities"; totals differ per role).

**Bands** (`bands.js`; fully diluted percent, `[lo, hi]`; every reachable role × class × stage cell is defined)

Founder (any role, joining = formation):

- `base = 100 / founders`; `lo = base − 15`, `hi = base + 15` for cto and engineer; `lo = base`, `hi = base + 15` for ceo_builder. Clamp both to `[1, 90]`. Values are percentage points.
- At stages after formation, multiply `lo` and `hi` by the product of `(1 − d)` over rounds already closed: seed → [seed]; series_a → [seed, A]; series_b_plus → [seed, A, B], with `d` from the dilution table below.

Non-founder bands, columns idea/pre-seed · seed · Series A · Series B+:
| role | class | pre-seed | seed | A | B+ |
|---|---|---|---|---|---|
| cto | founding_executive | 8–15 | 4–8 | 2–4 | 1–2 |
| cto | hired_executive | 3–6 | 1–5 | 1–3 | 0.5–1.5 |
| cto | hire | 0.5–1.5 | 0.25–0.75 | 0.1–0.5 | 0.05–0.25 |
| engineer | founding_executive | 3–7 | 1–3 | 0.5–1.5 | 0.25–0.75 |
| engineer | hired_executive | 1–2 | 0.5–1 | 0.2–0.6 | 0.1–0.3 |
| engineer | hire | 0.5–1 | 0.2–0.5 | 0.1–0.25 | 0.05–0.15 |
| ceo_builder | founding_executive | 10–20 | 5–10 | 3–5 | 1.5–3 |
| ceo_builder | hired_executive | 3–6 | 1–5 | 1–3 | 0.5–1.5 |
| ceo_builder | hire | 0.5–1.5 | 0.25–0.75 | 0.1–0.5 | 0.05–0.25 |

Sources per row in `benchmarks.js`: Index Ventures Rewarding Talent, Ravio 2026, founder-math CTO benchmarks, Stock Option Counsel, Carta 2026 medians (via Alumni Founders / EquityList). `updatedAt: '2026-09'`. Engineer rows are the least-sourced (open question 3).

**Market salary defaults** (`benchmarks.js`, USD base, US non-Bay-Area hubs; step 3 default only, always editable; sources Ravio 2026, founder-math, Kore1 2026)
| role | pre-seed | seed | A | B+ |
|---|---|---|---|---|
| cto | 120k | 150k | 190k | 230k |
| engineer | 110k | 135k | 160k | 185k |
| ceo_builder | 100k | 140k | 180k | 220k |

**Adjustments** (`adjustments.js`; percentage points, shown separately, added to both `lo` and `hi`; apply to every class including `founder`)

- Exchange rate by stage (`unitDollars → unitPts`): pre-seed $50k → 0.75; seed $50k → 0.5; Series A $100k → 0.75; Series B+ $100k → 0.4. `pts = (dollars / unitDollars) × unitPts`, linear. (Rule of thumb from founder-math: at seed every $50k of salary ≈ 0.5–1% equity.)
- Banked work: only when `joining === 'fractional_conversion'`, otherwise `bankedPts = 0` (salaried or founder contribution). `banked = max(0, months × 4.33 × hoursPerWeek × ratePerHour − feesBilled)`; `bankedPts = min(5, pts(banked))`.
- Salary: only when `offeredSalary` is present. `gap = marketSalary − offeredSalary`; `salaryPts = clamp(pts(gap × min(vestingYears, 2)), −2, +4)`.
- Combined cap: `adj = min(7, bankedPts + salaryPts)` (floor −2). Caps are stated in the "Show the math" panel.
- Instrument: no math; options vs restricted stock is flagged in the brief as a question to ask (tax and exercise cost).

**Offer read** (`engine.js`)

- `range = [lo + adj, hi + adj]`, rounded to 0.05 pt when `hi + adj < 2` and to 0.5 pt otherwise, then floored _after_ rounding to `[0.05, 0.1]` (14 of the 36 non-founder cells sit below 1%, so a coarse grain would print "0 to 0.5 percent"). The table-driven test asserts `0 < lo < hi` post-rounding for every cell.
- `position`: below / within / above when `offeredEquityPct` is present; `unknown` otherwise (gap bar empty, copy "Enter the offer to see the gap").
- `gapPts = [range.lo − offer, range.hi − offer]` when below.
- "The number to say": the midpoint of `range` rounded at the same grain as the range, with the range in the sentence. Template: "{Class} {role} work at {stage} sits at {lo} to {hi} percent fully diluted. I'm at {offer}." Ask for the midpoint. With no offer the sentence drops the "I'm at" clause.
- No offer entered: the dilution timeline and the exit scenarios use the range-midpoint series only, and the "Ask sanity" line is hidden.

**Dilution** (`dilution.js`; per-round median `d`, option-pool refresh included; Carta 2026 / Index)
| round | seed | A | B | C | D | IPO |
|---|---|---|---|---|---|---|
| d | 20% | 25% | 18% | 15% | 12% | 15% |

Remaining rounds by stage: idea/pre-seed → [seed, A, B, C, D, IPO]; seed → [A, B, C, D, IPO]; series_a → [B, C, D, IPO]; series_b_plus → [C, D, IPO]. Rounds applied by path: bootstrap → none (timeline renders "Today" only, copy "No priced rounds assumed"); acquisition → the first two remaining rounds; ipo → all remaining rounds. All three paths are always computed; the results toggle is always rendered and preselected to the declared path (acquisition when unknown), so the "Ask sanity" line always has both acquisition and IPO series. Output: stake after each applied round, starting from the offer when present, plus a comparison series starting from the range midpoint (the only series when there is no offer).

**Exit scenarios** (`scenarios.js`; dollars = stake at exit × exit valuation; not preference- or tax-adjusted; the three valuations are editable under "Show the math")
| path | conservative | base | upside |
|---|---|---|---|
| bootstrap / hold | 3M | 10M | 30M |
| acquisition | 25M | 100M | 400M |
| ipo | 400M | 1.5B | 5B |
"Ask sanity" line: the offered percent under acquisition vs IPO, base case, so a 20% ask at an IPO-bound company shows its absolute size.

### Components and files

- `src/app/(site)/founders/page.jsx` (desk landing, server), `src/app/(site)/founders/equity/page.jsx` (metadata) + `EquityCalculator.jsx` (client).
- `src/components/founders/desk-sidebar.jsx`, `wizard-shell.jsx` (steps, progress, Motion step transitions honoring reduced motion), `results-rail.jsx` (+ mobile summary bar), `live-announcer.jsx`, `steps/who-you-are.jsx`, `steps/the-work.jsx`, `steps/salary-and-offer.jsx`, `results/read.jsx`, `results/rounds-timeline.jsx`, `results/exit-scenarios.jsx`, `results/show-the-math.jsx`, `results/brief-actions.jsx`.
- shadcn `slider` added via `pnpm dlx shadcn@latest add slider` and scoped with `amw` like Select; `SliderField` wrapper pairs it with a number input. Chips are `button[aria-pressed]` using `.amw-chip`, with `.amw-chip--accent` as the pressed style (`.amw-chip` alone has no pressed state). Lucide icons for the sidebar marks.
- `PillNav`'s `isSubmenuActive` compares `pathname` to the raw `sub.href`, so on `/contact?topic=offer-review` the top-level Contact pill highlights rather than Founders' Desk. Accepted as-is for v1: the alternative (matching on pathname) would light both pills on every contact visit, and the comparison is duplicated in three places.
- Print: the "Show the math" panel is always mounted and toggled with `hidden print:block`, so it prints expanded. Page-level chrome (`AppHeader`, `Footer`, the rail, the wizard controls) is hidden by a global rule in `storefront.css`: `@media print { [data-print="hide"] { display: none !important } }`, with `data-print="hide"` added to the header and footer roots; the brief itself gets `opacity:1; transform:none` under the same media query. Nothing persisted.
- CTA: "Have AMWARE review your offer" links to `/contact?topic=offer-review`. `src/app/(site)/contact/page.jsx` reads `searchParams` (async prop in Next 15) and passes `topic` to `ContactForm` as a prop; the form prefills `subject = "Offer review"` when `topic === 'offer-review'`. No `useSearchParams`, no Suspense boundary, no schema change. No numbers are passed.
- Tests: vitest (new dev dependency) with `"test": "vitest run"` in `package.json` and a minimal `vitest.config.mjs` (node environment, `include: ['src/lib/**/__tests__/**']`). Table-driven tests over role × joining × stage assert every band cell resolves; explicit cases for the fractional-conversion wedge, the below/within/above sentence, formation always classifying as founder, the combined adjustment cap, and IPO vs acquisition round counts per stage.

### Accessibility and behavior

- Sliders keyboard-operable with `aria-valuetext` in units; the paired number input is the typed override.
- Live announcements via the hidden announcer only (see rail above).
- Chips meet 44px targets; step transitions respect `prefers-reduced-motion`.
- Empty/unknown states: "Don't know" on exit path computes all three scenario sets; a missing offer keeps the gap bar empty with copy, never a fake zero; a missing offered salary skips the salary adjustment and says so.

## Open Questions

1. Benchmarks in a versioned JS file (chosen for v1) vs a Payload global editable from the CMS. Revisit after the first benchmark refresh.
2. CEO-who-builds path: does it need capital-in and idea-origination inputs, or is the founder band plus responsibilities enough for v1?
3. Founding engineer bands are the least-sourced numbers in the table; confirm against Carta's engineer grant data before launch.
4. Whether the desk landing should also surface the Courses/Services that relate to founders, or stay tools-only.

## Success Criteria

- A first-time visitor sees a provisional band after step 1 and a classification after step 2, within 60 seconds of starting.
- The results screen is readable without the glossary: every term glossed inline on first use.
- Engine tests cover all role × joining × stage combinations and the named wedge cases; `pnpm lint`, prettier, and `vitest run` clean. (`tsc --noEmit` only covers `.ts/.tsx` in this repo, so it is not a criterion for these `.jsx` files.)
- Contact submissions with subject "Offer review" appear within the first month of launch (the lead-magnet proof).
- Zero request payloads carrying user inputs (verified in devtools before launch; pageview beacons are expected and carry no inputs).

## Dependencies

- vitest (dev) plus `vitest.config.mjs` and a `test` script; shadcn `slider`; lucide icons (present). No new runtime dependencies beyond the slider primitive.
- Benchmark table populated and sourced before the engine is finalized (see assignment).
- `contact/page.jsx` reads `searchParams` and `ContactForm.jsx` accepts a `topic` prop.
- `storefront.css` gains the `[data-print="hide"]` print rule; `AppHeader` and `Footer` roots get the attribute.

## The Assignment

Before writing the engine: collect three real fractional-to-full-time CTO offers from your network this week (stage, equity percent, salary vs market, months banked, declared exit path). Run each through the model by hand using the bands above. If the bands say all three offers are "below range," the bands are probably calibrated to the lived reality; if they say all three are "within," the bands are too generous and the tool will reassure people it should alarm.

## What I noticed about how you think

- You didn't take my recommended person. I offered "engineer recruited as technical co-founder"; you typed "Fractional CTO converting to full-time Pre-seed CTO." That is a more specific human than the one I proposed, and it defined the wedge.
- "I lived it myself" and "Undervalued my own contribution" came without hedging. Most people describe the market first and themselves never.
- When Codex challenged the exit-dollars premise you revised it immediately. You're not attached to the idea, you're attached to the outcome.
- "Make the sidebar include iconography for all the tools." You asked for marks on tools that don't exist yet. That's the same instinct that had you restore the border glow on the project cards an hour earlier: the details are the product.
